# 핀(==)은 깨끗한 Python 3.12 venv에서 설치 후 확정합니다.
# 설치: pip install -r requirements.txt && pip install -e .
# 선택적 웹 검색(DuckDuckGo)을 쓰려면 ddgs 도 설치됩니다(아래 포함).

langgraph>=0.2,<2.0
langchain>=0.3,<1.0
langchain-core>=0.3,<1.0
langchain-community>=0.3,<1.0
langchain-anthropic>=0.3,<0.4
langchain-google-genai>=2.0
langchain-chroma>=0.1
chromadb>=0.5
google-generativeai>=0.8
python-dotenv>=1.0
pydantic>=2.5
gradio>=4.0

# 선택: 라이브 웹 검색 (SEARCH_BACKEND=ddg 일 때만 필요)
ddgs>=6.0

# 개발/평가
pytest>=8.0
